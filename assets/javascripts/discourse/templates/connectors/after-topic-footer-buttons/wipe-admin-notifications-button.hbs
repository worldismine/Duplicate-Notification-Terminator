{{#if topic.isPrivateMessage}}
  {{#if currentUser.admin}}
    {{#if siteSettings.duplicate_notification_admin_btn_enabled}}
      {{d-button  action=(action "wipeNotifications")
                  class="btn-danger btn-large"
                  disabled=loading
                  label=(unless site.mobileView "duplicate_notification_terminator.wipe_notifications")
                  icon="bell-slash"}}
    {{/if}}
  {{/if}}
{{/if}}
